<script setup>
import DemoVideo from './demo/DemoVideo.vue'
</script>

# Demo

> Note: playback relies on the browser's native HLS support (e.g. Safari, iOS, and Edge on some platforms). Older browsers might not work properly.

<<< ./demo/DemoVideo.vue

<DemoVideo />
